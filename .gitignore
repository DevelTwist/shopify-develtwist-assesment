# --- Design source (must never be committed) ---
# The Figma reference export is a private design asset shared for the build only.
figma-design.jpg
figma-design.png
*.fig

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# --- Node (used only by the reference webhook example) ---
node_modules/
npm-debug.log*
.env
.env.*
# ...but keep the committed example env template
!.env.example
!**/.env.example

# --- Shopify CLI ---
.shopify/
*.theme-check-cache
